// const items = `required→true←Establishment name is mandatory&pattern→^[a-z]←Invalid Establishment name `;

// const isRegex = (str) => {
//     try {
//       const rex =new RegExp(str);
//       return true;
//     } catch (e) {
//       return false;
//     }
//   };